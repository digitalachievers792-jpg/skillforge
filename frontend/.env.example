# SkillForge Frontend env vars (Vercel: Settings → Environment Variables)
# Leave unset to call the API at the same origin (/api) — required when the
# backend serves the built frontend (single-origin Render deployment).
# Set this only when frontend and backend are on different domains:
# VITE_API_BASE_URL=https://your-backend.onrender.com
